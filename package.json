{
  "name": "viskar-particles-sandbox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "@types/three": "=0.130.1",
    "push-dir": "^0.4.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  },
  "packageManager": "yarn@4.5.0",
  "dependencies": {
    "three": "=0.131.2"
  }
}
